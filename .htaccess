RewriteEngine On

# 주소창에서 .html을 강제로 떼어버리는 설정
RewriteCond %{THE_REQUEST} /([^.]+)\.html [NC]
RewriteRule ^ /%1 [L,R=301]

# .html 없이 접속해도 실제 html 파일을 연결해주는 설정
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}\.html -f
RewriteRule ^([^\.]+)$ $1.html [NC,L]